# Example environment variables for BioLumina backend
# To enable the OpenAI integration, set OPENAI_API_KEY to your key
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo
# In production, run the Flask app behind an HTTPS proxy (nginx) and set proper secrets
